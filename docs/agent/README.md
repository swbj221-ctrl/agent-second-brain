# Agent Profile

Use these files to prime the assistant at the start of each session.

Suggested open-session snippet:
1. Read in order: `docs/agent/USER.md`, `docs/agent/IDENTITY.md`, `docs/agent/TOOLS.md`, `docs/agent/SOUL.md`.
2. Apply the preferences and constraints in all decisions.

Keep each file compact and stable. Avoid duplicating full docs or volatile details.
