# Progress

## Log
- 2026-02-26: Iteration-13 top-level chat source enforcement in `vault/.claude/skills/openclaw-main/adapter.py` for Telegram direct voice/media turns. At final response assembly, media/inferred turns now fail-closed unless text source is `bridge_stt`; provider transcript echoes are blocked with explicit safe text and `chat_response_source=provider_transcript_blocked`, non-bridge errors map to `chat_response_source=fallback_error`, successful bridge text maps to `chat_response_source=bridge_stt`. Added explicit top-layer log marker fields `chat_response_source` + `media_or_inferred` in `openclaw_adapter_outgoing_boundary` (visible in `openclaw logs --follow --json --plain`). Routing/anti-bypass/requestId strictness unchanged. Added targeted smoke `scripts/openclaw_chat_response_source_smoke.py`.
- 2026-02-26: Iteration-12 mixed-sequence retention fix in bridge multipass scoring (`src/d_brain/integrations/openclaw_bridge.py`): addressed RU+EN+RU truncation where trailing RU segment could be dropped in favor of shorter RU+EN/translit-like variants (for example `privyat how are you`). Added conservative lead/tail sequence metadata and a narrow score adjustment that prefers candidates preserving a trailing Cyrillic segment after the shared middle English run, while penalizing truncated competitors for the same middle signature. Routing, anti-bypass, and requestId-chain behavior unchanged. Added smoke `case=stt_multipass_mixed_full_sequence_not_truncated` in `scripts/openclaw_voice_dispatch_smoke.py`.
- 2026-02-26: Iteration-10 trace localization instrumentation only (no behavior changes). Added adapter final-send boundary log marker `openclaw_adapter_outgoing_boundary` in `vault/.claude/skills/openclaw-main/adapter.py` with correlation fields needed to disambiguate adapter-origin replies vs upstream channel relay sends on live Telegram: `reply_text_source`, `media_or_inferred`, `runId/sessionId`, `outgoing_text_preview_hash/len`, and `channel_send_origin_module`. Existing route markers (`adapter.pre_bridge` / `adapter.post_bridge`) and guard behavior unchanged.
- 2026-02-26: Iteration-9 last-mile hard cut in Telegram reply layer (adapter send decision) to block provider transcript echo leaks on voice/media turns. Root cause: final reply text in adapter could still pass through provider transcript text (`provider_transcript`) when media was present/inferred, allowing tails like `how are you` to appear as assistant output despite bridge-first STT pipeline. Fix: added top-level response guard in `vault/.claude/skills/openclaw-main/adapter.py` right before response return; for `media_or_inferred=true`, only `bridge_stt` text is allowed, while provider transcript is blocked (`reply_text_source=provider_transcript_blocked`) and forced to explicit fail-closed voice error text (`reply_text_source=fallback_error` for any non-bridge voice text). Added decision markers `reply_text_source` and `media_or_inferred` (alongside existing compatibility fields). Wrapper-first routing, anti-bypass, and requestId chain untouched.
- 2026-02-26: Iteration-6 channel/gateway pre-bridge fix for Telegram embedded voice tails (`Transcript: how are you`) being treated as final user text in some wrapper-detection misses. Root cause: wrapper audio detection under-classified embedded `<media:file>` cases when MIME/path extension was weak (`.oga` and file-tag+audio-body cases), allowing non-audio branch fallback where provider transcript tail could dominate visible output. Fixes: (1) broaden wrapper audio detection (`.oga` support + `<media:file>` with `[Audio] User text` treated as audio payload), preserving wrapper-first bridge routing; (2) add explicit final transcript source diagnostics marker in bridge/ingest path (`final_transcript_source` = `bridge_stt` or `provider_transcript`), surfaced through wrapper adapter diagnostics for live proof. Anti-bypass and strict requestId chain behavior unchanged. Verification: `py_compile` (wrapper + bridge), `openclaw_live_voice_bridge_cli_smoke.py`, targeted parse check for `.oga` embedded payload -> `selected_path=d_brain_openclaw_bridge`.
- 2026-02-26: Iteration-4 root-cause hardening for live Telegram inbound->embedded payload normalization. Root cause: `_normalize_voice_payload(...)` could treat raw embedded prompt text from `message.content` as user transcript when media existed but `text`/`transcript` was empty, allowing transcript-like EN tails to leak into selection context before/around normal media STT precedence. Fixes: (1) suppress embedded prompt-shaped `text/content` when media hints are present and no explicit transcript field exists, (2) add safe inbound diagnostics (`rawTextLen/hash/preview`, `rawContentLen/hash/preview`, `embeddedPromptSuppressed*`) to `telegram_stt_source_select` / `telegram_voice_ingest` / `telegram_voice_pipeline`, and (3) enforce media-derived STT source precedence in `dispatch_voice` when bytes are available (`stt_source` cannot stay `transcript` in normal voice/audio paths). Wrapper-first anti-bypass and strict requestId chain preserved. Added smoke case `embedded_prompt_in_content_is_suppressed_for_voice_media` in `scripts/openclaw_voice_source_select_smoke.py`.
- 2026-02-26: Iteration-3 live path fix for mixed RU+EN Telegram voice where RU lead was dropped and only English tail survived. Root cause: wrapper forwarded embedded transcript text on audio-media path; in live mismatches this could behave like transcript-first fallback before/around STT and bias output toward the English tail. Fix: `scripts/openclaw_live_voice_bridge_cli.py` now enforces media-first by default (`embeddedTranscriptDecision=suppress_embedded_transcript`, reason `media_first_stt`) and only allows transcript forwarding via explicit diagnostic env `OPENCLAW_EMBEDDED_TRANSCRIPT_FORWARD=1|true|yes|on`. Added bridge pre-multipass observability stage `telegram_voice_pipeline` -> `stt_pre_multipass_source` in `src/d_brain/integrations/openclaw_bridge.py` to prove whether a request entered as transcript-only vs media-backed before multipass selection. Anti-bypass and strict requestId-chain behavior unchanged.
- 2026-02-26: Iteration-2 mixed RU+EN retention hardening for live voice multipass selection: patched `src/d_brain/integrations/openclaw_bridge.py` candidate scoring to detect same-tail mixed phrases and strongly prefer Cyrillic-leading candidates when competing latin-only lookalikes collapse short RU lead tokens (for example `priya/riviere how are you` vs `Привет how are you`). This is a narrow scoring change only; wrapper-first anti-bypass path and strict same-`requestId` proof chain remain unchanged. Added smoke coverage `case=stt_multipass_mixed_short_ru_lead_not_collapsed` in `scripts/openclaw_voice_dispatch_smoke.py`.
- 2026-02-26: Fixed final NO-GO blocker for live Telegram voice trace verdict discoverability caused by PowerShell `Tee-Object` UTF-16LE log captures. `scripts/openclaw_live_voice_path_verdict.py` now decodes `--log-capture` files with UTF-16/BOM/zero-byte heuristics before marker scanning, so early-stage markers (`runtime.exec_guard` / `redirect.path_select` / `wrapper.path_select` / `wrapper.adapter_dispatch`) remain discoverable in `openclaw logs --follow --json --plain` artifacts without relaxing strict same-requestId acceptance. Added UTF-16 coverage to `scripts/openclaw_live_voice_path_verdict_smoke.py` (`bridge_confirmed_from_utf16_log_capture`).
- 2026-02-25: Improved live log-capture discoverability for embedded voice proof-chain markers without changing routing logic. Redirect shim and wrapper CLI now mirror path proof/error/trace markers as standalone JSON lines to `stderr` (safe for exec stdout JSON parsing) and emit both `requestId` + `request_id` keys with the same value; redirect shim also forwards wrapper `stderr` marker lines so `openclaw logs --follow --json --plain` can capture the full chain more reliably. Adapter pre/post bridge path markers now also emit both requestId key styles. Verdict report now includes concise per-stage source diagnostics (`log_capture` / `session_only` / `missing`) for chain-stage troubleshooting. Verification PASS: `py_compile` + `openclaw_embedded_media_redirect_smoke.py` + `openclaw_live_voice_bridge_cli_smoke.py` + `openclaw_live_voice_path_verdict_smoke.py`.
- 2026-02-25: Tightened `openclaw_live_voice_path_verdict.py` live-run scoping to prevent synthetic/session-tail contamination in production verdicts. Bridge confirmation now requires the selected `full_chain_request_id` to be discoverable in the provided `--log-capture` (raw log lines), and direct Deepgram bypass (`session_deepgram_direct=true`) is now scoped to live-run evidence (log capture Deepgram marker or session Deepgram evidence carrying a requestId also seen in the log capture). Strict same-requestId chain acceptance is unchanged. Added verdict smokes for session-only fixture chain rejection and stale session Deepgram non-scope. Verification PASS: `openclaw_live_voice_path_verdict_smoke.py`.
- 2026-02-25: Fixed embedded Telegram voice strict-chain requestId continuity in repo source without refactoring the transport path. Root cause in repo path: `dispatch_voice_sync(...)` accepted `request_id` but did not pass it into async `dispatch_voice(...)`, which could break bridge-stage request correlation. Patch preserves canonical requestId across `dispatch_voice_sync -> dispatch_voice`, normalizes `requestId`/`request_id` at redirect wrapper / wrapper CLI / adapter / bridge boundaries, and keeps fail-closed `request_id_missing_in_chain` behavior. Also hardened `scripts/openclaw_live_voice_path_verdict.py` wrapper-stage extraction for JSON spacing/escaping variants (strict same-requestId chain requirement unchanged). Local verification PASS: `py_compile` (bridge/adapter/wrapper/redirect/verdict), `openclaw_embedded_media_redirect_smoke.py`, `openclaw_live_voice_bridge_cli_smoke.py`, `openclaw_live_voice_path_verdict_smoke.py`, `openclaw_voice_source_select_smoke.py`, `openclaw_voice_dispatch_smoke.py`. Live Telegram strict acceptance still pending a fresh voice capture after restart.
- 2026-02-25: Runtime bypass-kill hardening for embedded Telegram audio in installed OpenClaw `pi-embedded` bundles (outside repo source): widened direct-Deepgram exec detection from a single endpoint pattern to any Deepgram command signature (`deepgram` / `deepgram.com`) for exec-like tools under embedded audio prompts, so normal Telegram audio/voice/audio-document prompts are rewritten to the wrapper-first bridge path by default. Added explicit diagnostic-mode override flag `OPENCLAW_AUDIO_DIRECT_DEEPGRAM_DIAGNOSTIC=1|true|yes` (only when intentionally enabled) that logs `selectedPath=embedded_direct_deepgram_diag`; default mode remains fail-closed to wrapper/bridge path with existing `wrapper_bridge_unavailable` error marker on shim/python resolution failure. Verification PASS: `node.exe --check` on both patched `pi-embedded` bundles. Live Telegram strict proof still pending fresh voice sample after restart.
- 2026-02-25: Final-proof-gap hardening for live voice path verdict tooling (no routing logic changes): compacted workspace `HEARTBEAT.md` / `BOOTSTRAP.md` to reduce OpenClaw embedded injected-context truncation risk, then fixed `scripts/openclaw_live_voice_path_verdict.py` request-scoped marker extraction for runtime-style request IDs containing `|` (for example `oc-...|fc-...`) and nested OpenClaw log `raw` payload JSON. Verdict parser now scans nested `raw/message` blobs plus decoded JSON dumps, still requires a strict same-`requestId` full chain, and honors `--evidence-limit` in JSON output to keep reports manageable. Added focused verdict smokes in `scripts/openclaw_live_voice_path_verdict_smoke.py` for (1) full same-requestId chain in nested `raw` logs with pipe-containing requestId -> `LIVE_PATH_CONFIRMED_BRIDGE`, and (2) missing-stage fixture -> `INCONCLUSIVE`. Verification PASS: `openclaw_live_voice_path_verdict_smoke.py`. Live Telegram single-sample strict proof still pending manual voice send in this session.
- 2026-02-25: Second-pass live Telegram voice-path tracing hardening after mixed phrase still transliterated in real Telegram (`Привет, how are you, ты меня понимаешь?` -> `priyad how are you`). Added request-scoped stage markers across workspace redirect/wrapper/adapter path to prove routing branch end-to-end: redirect shim markers now include `traceStage=redirect.path_*` + `requestId`; wrapper markers include `traceStage=wrapper.path_*` + `requestId`; wrapper now emits `openclaw_voice_dispatch_wrapper_trace` with explicit media-vs-embedded-transcript forwarding decision (`embeddedTranscriptForwarded`, `embeddedTranscriptDecision`, suppression reason); adapter now emits `openclaw_voice_dispatch_path_select` (`traceStage=adapter.pre_bridge`) and `openclaw_voice_dispatch_path_result` (`traceStage=adapter.post_bridge`) with STT source/fallback/multipass summary. Redirect shim now hard-fails audio prompts if wrapper reports a non-bridge selected path (`wrapper_unexpected_non_bridge_path`) to reduce hidden policy bypass risk. Extended smokes: `openclaw_live_voice_bridge_cli_smoke.py`, `openclaw_embedded_media_redirect_smoke.py` (including wrapper non-bridge hard-block case), and `openclaw_live_voice_path_verdict.py` metadata extraction for selected multipass/translit evidence. Verification PASS: `py_compile` (wrapper/redirect/verdict/adapter), `openclaw_live_voice_bridge_cli_smoke.py`, `openclaw_embedded_media_redirect_smoke.py`, `openclaw_live_voice_path_verdict_smoke.py`. Live Telegram verification still pending.
- 2026-02-25: Hardened OpenClaw bridge mixed-language Telegram voice transcription selection and transcript-only safety in repo source (`src/d_brain/integrations/openclaw_bridge.py`). Changes: enforced strict media-priority for normal Telegram `voice`/`audio`/audio-`document` payloads (no silent transcript-only fallback when media is declared but malformed), strengthened multipass STT heuristic for mixed RU+EN (script/lang mismatch penalties, Cyrillic-preservation bonus, transliterated pseudo-Russian Latin noise penalty, mixed-script preference), and added transcript-only clarification behavior for likely keyboard-layout typos / translit-like garbage while preserving valid English text without forced normalization. Added grep-friendly diagnostics (`telegram_voice_pipeline` stages `transcript_only_fallback` / `transcript_only_clarify`, transliteration score fields in multipass candidate/selected logs). Updated smoke coverage in `scripts/openclaw_voice_dispatch_smoke.py` and `scripts/openclaw_voice_source_select_smoke.py` for mixed phrase retention, Cyrillic RU preservation, layout typo confirmations (`ghbdtn` / `руддщ`), valid English transcript-only no-clarification, and strict media-priority malformed voice payload behavior. Verification PASS: `py_compile` (bridge + both smokes), `openclaw_voice_dispatch_smoke.py`, `openclaw_voice_source_select_smoke.py`.
- 2026-02-25: Diagnosed and patched OpenClaw `voice-call` streaming STT EN-bias transliteration in the installed runtime extension (`C:\Users\User\AppData\Roaming\npm\node_modules\openclaw\extensions\voice-call`, outside repo source). Root cause: active Twilio media-stream -> OpenAI Realtime STT path sent `transcription_session.update` with model only (no language hint, no transcription prompt), which caused Russian and mixed RU+EN speech to be transcribed as English-phonetic Latin transliteration while EN-only remained acceptable. Applied a minimal patch to the active `voice-call` streaming branch only: added config-backed `streaming.sttLanguage` (default `ru`) and `streaming.sttPrompt` (bilingual anti-transliteration prompt), passed both into `input_audio_transcription` for the OpenAI Realtime session update, and added short safe diagnostics showing selected STT provider/model/language/prompt-set plus final/raw transcript previews before agent processing. Extended plugin UI/schema hints in `index.ts` and `openclaw.plugin.json` for the new streaming config fields. Verification in this session: source inspection/grep only (no live voice-call execution yet). Remaining work: restart OpenClaw, run RU-only / EN-only / mixed RU+EN voice-call checks, and confirm transcript logs contain Cyrillic for RU segments without transliteration.
- 2026-02-25: Hardened embedded OpenClaw audio/voice transcript encoding handling for the wrapper-first path to prevent mojibake token patterns from leaking into fallback text/diagnostics. Updated `scripts/openclaw_live_voice_bridge_cli.py` to support `TranscriptB64` / `TranscriptBase64` UTF-8 decode, explicit `EMPTY_TRANSCRIPT` status, mojibake detection heuristics, UTF-8 stdio reconfiguration, and diagnostic fields (`embedded_transcript_status`, `embedded_transcript_decode_mode`, `embedded_transcript_len`, `embedded_transcript_base64_decoded`, `embedded_transcript_mojibake_suspected`). Wrapper now suppresses suspicious mojibake transcript text before adapter handoff so media STT remains the source of truth. Updated `scripts/openclaw_embedded_media_redirect_cli.py` to force `PYTHONUTF8=1` / `PYTHONIOENCODING=utf-8` for wrapper subprocess execution. Extended `scripts/openclaw_live_voice_bridge_cli_smoke.py` with RU UTF-8 base64 decode, mixed RU+EN transcript parsing, explicit empty transcript status, mojibake detection, and anti-forwarding checks for corrupted transcript text. Also updated the installed OpenClaw `pi-embedded` runtime hotfix (outside repo source) to prepend PowerShell UTF-8 console/output setup plus Python UTF-8 env vars before invoking the redirect shim. Verification PASS: `py_compile` (wrapper + redirect shim + smoke), `openclaw_live_voice_bridge_cli_smoke.py`, `node.exe --check` on both patched `pi-embedded` bundles.
- 2026-02-25: Implemented a minimal local OpenClaw runtime hard-enforcement hotfix (outside repo source, in installed npm bundle) for embedded Telegram audio/voice bypass to direct Deepgram `exec`. Patched `runBeforeToolCallHook(...)` in both installed `pi-embedded-*.js` bundles under `C:\Users\User\AppData\Roaming\npm\node_modules\openclaw\dist\` to detect direct Deepgram STT `exec`/`bash` commands (`api.deepgram.com/v1/listen`), inspect latest `chat.history` user prompt for embedded audio media markup, and rewrite the command to the repo redirect shim (`scripts/openclaw_embedded_media_redirect_cli.py --message-file <tmp>`) instead of allowing direct STT `exec`. The runtime guard emits proof/error markers (`openclaw_voice_dispatch_path_select` / `openclaw_voice_dispatch_path_error`) and blocks with a safe diagnostic error when shim/python path cannot be resolved (no silent direct fallback). Optional env overrides added by patch for local path resolution: `OPENCLAW_AUDIO_REDIRECT_SHIM`, `OPENCLAW_AUDIO_REDIRECT_PY`, `OPENCLAW_AUDIO_REDIRECT_REPO`. Verification PASS: `node.exe --check` on both patched `pi-embedded` bundle files. Live Telegram verification pending (marker order + verdict script).
- 2026-02-25: Prepared a minimal core OpenClaw runtime patch plan (no core code changes in this repo) for the embedded Telegram audio/voice bypass confirmed by session JSONL (`toolCall exec` + direct `api.deepgram.com/v1/listen`). Based on repo grep + live evidence, the highest-confidence patch-point is an embedded tool-dispatch/exec guard before `toolCall exec` execution; fallback patch-point is embedded planner tool-policy/prompt enforcement. Documented priority patch-points, proof/error markers, safe fallback behavior, and a temporary repo hardening workaround (post-facto verdict detection + explicit diagnostic message when verdict is `LIVE_PATH_BYPASS_EXEC_DEEPGRAM`) in the runbook and heartbeat. Verification: docs-only update (no runtime code changes).
- 2026-02-25: Added a minimal workspace-level embedded media redirect shim `scripts/openclaw_embedded_media_redirect_cli.py` to reduce live Telegram audio/voice bypass risk without changing OpenClaw core runtime. Repo grep confirmed no project-side direct Deepgram `exec` hook (bypass is formed in embedded-agent planning/tool-use path), so the narrow patch-point is workspace glue/instructions. The new shim detects embedded audio vs non-audio media, emits structured redirect proof/error markers (`openclaw_voice_dispatch_path_select` with `selectedPath=wrapper_cli_bridge|embedded_default_flow`, `openclaw_voice_dispatch_path_error` with `intendedPath=wrapper_cli_bridge`), and invokes the existing wrapper CLI (`scripts/openclaw_live_voice_bridge_cli.py --message-text ...`) as the only bridge route for audio media. Added deterministic smoke `scripts/openclaw_embedded_media_redirect_smoke.py` for audio->wrapper invoke, non-audio skip, and wrapper-unavailable anti-silent-bypass fallback; updated workspace AGENTS and runbook marker order guidance. Verification PASS: `py_compile` (redirect shim + smoke), `openclaw_embedded_media_redirect_smoke.py`, `openclaw_live_voice_bridge_cli_smoke.py`, `openclaw_live_voice_path_verdict_smoke.py`.
- 2026-02-25: Added one-shot live proof workflow support for wrapper-first Telegram voice/audio verification. New `scripts/openclaw_live_voice_path_verdict.py` performs best-effort evidence scan across log capture + OpenClaw session JSONL and returns `LIVE_PATH_CONFIRMED_BRIDGE` / `LIVE_PATH_BYPASS_EXEC_DEEPGRAM` / `INCONCLUSIVE` with concise evidence flags; tiny smoke `scripts/openclaw_live_voice_path_verdict_smoke.py` covers confirmed-bridge and exec+Deepgram bypass cases. Runbook now includes a copy/paste one-shot PowerShell sequence (restart + multipass env + log capture + marker grep + session tail + verdict script), and workspace bootstrap/AGENTS guardrails now require wrapper-first with visible proof fields and no direct Deepgram `exec` replacement unless explicit diagnostic mode is requested. Verification PASS: `py_compile` (verdict + smoke), `openclaw_live_voice_path_verdict_smoke.py`.
- 2026-02-25: Hardened embedded OpenClaw audio bridge wrapper proof/diagnostics for live path confirmation. `scripts/openclaw_live_voice_bridge_cli.py` proof marker now includes `messageKind` + `isAudioDocument` in wrapper JSON result/stdout, and audio-media bridge dispatch failures now return explicit `error_code=bridge_dispatch_unavailable` with anti-silent-bypass marker `openclaw_voice_dispatch_path_error` (`selectedPath=none`, `intendedPath=d_brain_openclaw_bridge`) plus safe user fallback (no direct STT fallback). Expanded `openclaw_live_voice_bridge_cli_smoke.py` to validate observable proof fields for audio/non-audio branches and the anti-silent-bypass marker on forced bridge loader failure. Updated runbook/integration docs with a short live verification command set (restart + multipass env + log capture + marker checks + session JSONL bypass check). Verification PASS: `py_compile` (wrapper + smoke), `openclaw_live_voice_bridge_cli_smoke.py`.
- 2026-02-25: Diagnosed live Telegram voice/audio bypass path in OpenClaw runtime: embedded agent was handling `<media:audio>` via ad-hoc `exec` + direct Deepgram STT (visible in OpenClaw session JSONL tool calls), so `d_brain.integrations.openclaw_bridge` markers (`telegram_voice_pipeline`, `stt_multipass_*`) never appeared in `openclaw logs`. Added a narrow embedded-media bridge wrapper `scripts/openclaw_live_voice_bridge_cli.py` that parses OpenClaw embedded Telegram audio prompts, emits proof marker JSON `openclaw_voice_dispatch_path_select` (`d_brain_openclaw_bridge` vs `embedded_direct_stt`), routes audio/voice/audio-document payloads into `vault/.claude/skills/openclaw-main/adapter.py -> d_brain` bridge, and returns safe diagnostic fallback on bridge import/runtime failure (no silent direct-STT fallback). Added adapter proof marker `openclaw_voice_dispatch_path_select` for the media->bridge branch and parser/route smoke coverage in `openclaw_live_voice_bridge_cli_smoke.py`. Updated workspace bootstrap/AGENTS and docs to require wrapper-first handling for embedded `<media:audio>` paths. Verification PASS: `py_compile` (wrapper/smoke/adapter), `openclaw_live_voice_bridge_cli_smoke.py`.
- 2026-02-25: Added opt-in non-tutor Telegram STT multipass decoding in `openclaw_bridge` for mixed RU+EN voice/audio quality troubleshooting without changing default behavior. New env flags: `TELEGRAM_STT_MULTIPASS`, `TELEGRAM_STT_MULTIPASS_LANGS`, `TELEGRAM_STT_MIXED_HEURISTIC`; explicit `TELEGRAM_STT_LANGUAGE` now safely skips multipass (logged) to preserve operator intent. Multipass emits structured `telegram_voice_pipeline` stages (`stt_multipass_start/candidate/selected/skip`) and uses a lightweight transcript scoring heuristic (length/tokens/script mix/repetition penalty). Added deterministic smoke coverage for multipass selection, disabled behavior parity, CSV lang parsing, and multipass logging in `openclaw_voice_dispatch_smoke.py`. Verification PASS: `py_compile`, `openclaw_voice_dispatch_smoke.py`, `openclaw_fallback_smoke.py`, `openclaw_voice_source_select_smoke.py`.
- 2026-02-25: Added deterministic `ffmpeg_missing` voice pipeline smoke coverage (no real ffmpeg dependency) by monkeypatching bridge `shutil.which` in `openclaw_voice_dispatch_smoke.py` with `TELEGRAM_STT_NORMALIZE_AUDIO=1`. Smoke now asserts exact `pipeline_error_code=ffmpeg_missing`, safe user fallback text (no traceback / internal ffmpeg details), and preprocess structured log stage (`telegram_voice_pipeline`, `stt_preprocess_error`) with `normalizeAudioEnabled=true`, `ffmpegPathFound=false`, and `inputMimeType=audio/ogg`. Also strengthened conversion-failure smoke assertions and expanded `telegram_voice_pipeline` payload with normalization diagnostics fields (`normalizeAudioEnabled`, `ffmpegPathFound`, `inputMimeType`, `inputExt`) while preserving log compatibility. Verification PASS: `openclaw_voice_dispatch_smoke.py`, `openclaw_voice_source_select_smoke.py`, `openclaw_fallback_smoke.py`.
- 2026-02-25: Telegram voice/audio/document pipeline diagnostics hardening (OpenClaw + aiogram handler path): confirmed bridge supports `message.voice` / `message.audio` / audio `message.document`, added structured pipeline debug logs (`telegram_voice_pipeline`) for download/preprocess/STT stages (message kind, MIME, download outcome, ffmpeg args/exit, transcript preview, STT language/provider, fallback/pipeline error codes), added optional audio normalization hook for STT (`TELEGRAM_STT_NORMALIZE_AUDIO=1` -> ffmpeg to mono 16k PCM WAV; safe fallback/error logging if ffmpeg missing/conversion fails), and added non-tutor STT language override env `TELEGRAM_STT_LANGUAGE`. OpenClaw adapter `document` media hint routing now treats only audio documents as voice candidates (non-audio documents stay on normal document path). Aiogram `voice.py` handler now logs `get_file` / download stages and audio metadata (`voice`/`audio`/audio `document`) and avoids catching non-audio documents in the voice router predicate. Added smoke coverage for env language override and synthetic conversion-failure fallback in `openclaw_voice_dispatch_smoke.py`. Verification PASS: `py_compile` (bridge/adapter/voice handler/smoke), `openclaw_voice_source_select_smoke.py`, `openclaw_voice_dispatch_smoke.py`, `openclaw_fallback_smoke.py`.
- 2026-02-25: Runtime import-path diagnostics + wiring hardening for OpenClaw voice bridge: added one-time bridge module path log (`event=openclaw_bridge_runtime_module_path` with `moduleFile`, `cwd`, `sysPathHead`, `pid`) and adapter-side Telegram voice dispatch entry log (`event=openclaw_bridge_runtime_dispatch_entry` with `moduleFile`, `hasVoice/hasAudio/hasDocument`). Hardened OpenClaw adapter import wiring to prepend repo `src` to `sys.path` based on `vault/.claude/skills/openclaw-main/adapter.py` location before importing `d_brain`, to reduce risk of loading a stale installed/duplicate `d_brain` tree. Local checks: no duplicate `openclaw_bridge.py` in workspace, no `d_brain` package installed in `.venv` site-packages; import with `PYTHONPATH=src` resolves to repo path `D:\\openclaw_bot\\agent-second-brain\\src\\d_brain\\integrations\\openclaw_bridge.py`. Verification PASS: `py_compile` (bridge + adapter), `openclaw_voice_source_select_smoke.py`, `openclaw_voice_dispatch_smoke.py`, `openclaw_fallback_smoke.py`.
- 2026-02-25: Targeted Telegram voice-note STT fallback UX hardening in OpenClaw bridge: added a final voice-note-safe fallback text override (never ask normal `message.voice` users to resend as `.ogg/.m4a`/file), centralized fallback text selection in bridge response finalization, strengthened `telegram_voice_note` inference (voice object / `messageKind=voice` / `hasVoice`), guaranteed structured voice-note fallback logs via shared ingest log helper, and expanded structured voice logs with `isVoiceNote`, `sttProvider`, `sttModel` plus stable final outcome fields. Added one-time runtime marker log (`event=openclaw_bridge_voice_fix_loaded`, `bridgeFile`, `voiceFixRev=2026-02-25b`) to confirm patched module loading in live runs. Expanded `openclaw_voice_source_select_smoke.py` with no-file-resend regression cases and structured-log assertion for voice-note fallbacks. Local verification: `py_compile` PASS, `openclaw_voice_source_select_smoke.py` PASS, `openclaw_voice_dispatch_smoke.py` PASS, `openclaw_fallback_smoke.py` PASS.
- 2026-02-24: Final hardening/observability patch for Telegram voice-note STT in OpenClaw bridge after source-priority fix: added explicit evidence fields to `telegram_stt_source_select` / `telegram_voice_ingest` (`requestId`, `userIdHash`, `messageKind`, Telegram file-id flags, downloader metadata, media bytes/path metadata, `transcriptLen`, `transcriptLooksAuto`, `finalInputSource`, `finalOutcome`, `responseMode`), normalized `fallback_reason` to a stable whitelist taxonomy, and added lightweight in-memory counters for voice source/fallback diagnostics (`telegram_voice_source.*`, `telegram_voice_fallback.*`) via existing observability counters. Added operator helper `scripts/openclaw_voice_log_summary.py` (file or `openclaw logs --tail N` -> per-`requestId` summary) and expanded `openclaw_voice_source_select_smoke.py` with edge regressions (nested bytes, malformed voice shape + transcript fallback, downloader exception, downloader bytes overriding transcript, document audio/ogg regression check). Local verification: compile PASS, `openclaw_voice_source_select_smoke.py` PASS, `openclaw_voice_dispatch_smoke.py` PASS, `openclaw_fallback_smoke.py` PASS. TTS empty-output guard path preserved.
- 2026-02-24: Fixed unstable Telegram voice note (`message.voice`) STT routing in OpenClaw bridge path by enforcing explicit source priority (`voice` -> `audio` -> audio `document` -> transcript fallback), adding nested media payload parsing + optional message-level downloader callback support for `file_id`, and preventing transcript-only short-circuit when a voice note exists. Added structured source-selection/ingest diagnostics logs (`telegram_stt_source_select`, `telegram_voice_ingest`) with media/download/STT/fallback fields, voice-note-safe retry messages (no ".ogg/.m4a file" request), and `empty_transcript_voice_note` fallback handling. Added `scripts/openclaw_voice_source_select_smoke.py` covering 5 required cases (voice file preferred over transcript, download fail no file request, empty transcript retry, audio file path still works, transcript-only fallback without media). Verified local smokes via `.venv`: `openclaw_voice_source_select_smoke.py` PASS, `openclaw_fallback_smoke.py` PASS, `openclaw_voice_dispatch_smoke.py` PASS. TTS empty-output guard path unchanged.
- 2026-02-25: Fixed `scripts/start_openclaw_stack.ps1` foreground OpenClaw startup path for Windows PowerShell: normalized process-scope PATH entries (strip stray quotes, add `C:\Program Files\nodejs` and `%APPDATA%\npm`), added explicit `Get-Command` checks/diagnostics for `node` and `openclaw`, passed `gateway --port <port>` via call operator `&`, and suppressed detached/foreground hints on foreground launch failure. Parse check passed (`parse_ok`). Live gateway foreground validation pending on MSI.
- 2026-02-25: Fixed OpenClaw Telegram TTS empty-output regression path in `vault/.claude/skills/openclaw-main/adapter.py` with an early media guard (bytes/path existence + size validation) that clears `audio_intent` and forces safe text fallback before transport media send handoff; added structured warning log `reason=empty_tts_output` for guard hits. Expanded `scripts/openclaw_fallback_smoke.py` with `adapter_empty_tts_output_guard` (zero-size file path) and `tts_valid_nonempty_media_path` cases. Smoke checks run: `.\.venv\Scripts\python.exe scripts\openclaw_fallback_smoke.py` -> PASS. Incident captured in `docs/learnings.md`. No `docs/incidents.md` / `docs/bugs.md` file exists in this repo.
- 2026-02-24: Added final pre-signoff live Telegram acceptance prep for OpenClaw-first prod path: created `docs/live-telegram-acceptance-template.md` and `ops/capture-live-signoff.ps1` (sanitized evidence capture to `artifacts/signoff/` with operator placeholders), expanded `docs/cutover-checklist.md` with explicit Live Telegram Acceptance Evidence and final GO gating rule, and updated runbook/integration/workspace continuity notes. No architecture changes; OpenClaw remains the only Telegram transport in prod and aiogram polling stays dev-only.
- 2026-02-24: Ran final cutover verification (local/no-polling evidence) for OpenClaw-first prod path: readiness/diag/RC/command/voice/fallback/duplicate-guard checks passed, OpenClaw gateway process confirmed listening on `18789` (`node.exe`), and bridge command acceptance verified via `ux_cli.py` for `/help`, `/status`, `/plan list`, `/plan add`, `/diag`, `/ping`, `/version`. Live Telegram channel and manual Telegram voice acceptance remain pending in this environment, so production sign-off is documented as pending manual operator confirmation. Added `docs/cutover-checklist.md` and `docs/release-notes-v1.0-draft.md`, and updated runbook cutover section.
- 2026-02-24: Added workflow governance docs for OpenClaw-first continuity: new `docs/learnings.md` (self-improvement log) and `docs/skill-contract.md` (internal skill creation/update contract), plus runbook/integration/workspace bootstrap+heartbeat updates for mandatory docs contract and session open/close continuity protocol. No runtime architecture changes; OpenClaw remains the only Telegram transport in prod and aiogram polling remains dev-only.
- 2026-02-24: v1.4 RC hardening pass (bridge-level): added sanitized timeout/error logging for sidecar/STT/TTS exception paths, exposed unified timeout values in `/diag full`, tightened command safety guards for extra args and oversized command inputs (short RU usage/fallback responses, no bridge API changes), refined offline readiness env checks to warn only for enabled features, expanded command/voice smokes for edge cases (long input, weird unicode, media-over-transcript priority, `sendAudio` intent), and added unified no-polling RC smoke suite (`scripts/openclaw_rc_smoke.py`) covering command/prefs/diag/voice/no-polling/readiness checks.
- 2026-02-24: v1.3 observability + admin quick controls (bridge-level): expanded `/diag` with short and `/diag full` modes (OpenClaw transport status, user prefs summary, STT/TTS enabled flags, sidecar availability probe, uptime/process start, runtime error counters/recent failures, commit hash fallback without secrets), improved `/ping` with lightweight timing and `/version` with commit hash fallback, normalized structured bridge/handler logs (`handler`, `source`, `action`, `status`, `error_type`, `duration_ms`), added lightweight in-memory runtime error tracker (counters + last error + recent ring buffer), added `scripts/openclaw_diag_smoke.py` and `scripts/openclaw_error_counter_smoke.py`, and expanded command smoke coverage for `/diag` and `/diag full`; compile + smokes passed locally via `.venv`.
- 2026-02-24: v1.2 preferences + reliability finish (bridge-level, transport-agnostic): added per-user preferences for `language_mode` (`ru|en_tutor`), `voice_reply` (`on|off`), and `brevity` (`short|normal`) using the existing session JSONL store; added `/prefs` commands (`show`, `brevity`, `lang`, `voice`) with `/voice on|off|status` alias compatibility; applied preferences in bridge voice dispatch (language mode -> tutor/STT EN default, voice_reply -> skip TTS, brevity -> shorter fallback/help warnings); synchronized `scripts/ux_cli.py help` to use the bridge `/help` path; added sidecar timeout/exception guard with structured degraded logs for plan handlers; added `scripts/openclaw_prefs_smoke.py` and expanded command/voice smokes for prefs cases; compile + smokes passed locally via `.venv`.
- 2026-02-24: v1.1 post-release hardening + UX polish: added bridge commands `/ping` and `/version`, explicit per-user voice reply preference (`/voice on|off|status`) via session-store-backed preference entries, short-TTL duplicate guard on bridge command/voice entrypoints with `duplicate_skipped=true` logging, and duplicate-guard smoke coverage (`scripts/openclaw_duplicate_guard_smoke.py`). TTS observability logs now include method/path/size/provider and fallback reasons.
- 2026-02-24: Prod MVP ready (OpenClaw-first Telegram transport, d_brain bridge/services, command+voice bridge stabilized, offline readiness + E2E smokes passed). Known limitations: transcript-only quality depends on Telegram transcript input; empty TTS output falls back to text; exactly one Telegram poller must run (OpenClaw only in production).
- 2026-02-24: Final stabilization pass before production use: added `scripts/prod_readiness_check.py` (offline PASS/WARN/FAIL summary for OpenClaw-first transport mode, bridge imports, workspace bootstrap/heartbeat files, and env readiness warnings), added `scripts/openclaw_e2e_stabilization_smoke.py` matrix (command/text/voice/TTS fallback), and hardened OpenClaw adapter audio payload validation/logging (`audio_intent`, `audio_path`, exists/size, fallback-only on invalid media). Verified `.venv` checks: no-polling-when-disabled, prod readiness, command smoke, voice smoke, stabilization matrix.
- 2026-02-24: Added production command MVP contract for OpenClaw bridge (`/help`, `/status`, `/mode`, `/plan add/list/done/delete`) with concise RU UX, stable plan done/delete parsing, soft-delete via existing `event_update_status=status=canceled`, and unified bridge response normalization (`text/audio_intent/audio_path/meta/ok/error_code` + legacy compatibility fields); extended `openclaw_command_dispatch_smoke.py` and verified offline via `.venv` smoke + `ux_cli.py command /mode`.
- 2026-02-24: Close-session update: completed OpenClaw-first observability + operations hardening slice (transport-agnostic health snapshot, `/health` + `/diag`, CLI `health/diag`, single-poller conflict guard hints, Windows `ops/*` start/stop/restart/status/PATH/conflict/ACL helpers, `ops_smoke.py`), updated runbook recovery sections, and verified compileall + ops/health/diag smokes via `.venv`.
- 2026-02-24: Added OpenClaw-first E2E routing smoke (`scripts/openclaw_e2e_smoke.py`) with JSONL output and end-to-end command/voice/job + non-fatal ingestion/index fallback cases; added runbook section and verified no-network coverage via `.venv`.
- 2026-02-24: Added production hardening guardrails for OpenClaw-first runtime: startup no-conflict local polling guard, structured JSON-friendly bridge/job/adapter logs, STT/TTS timeout and empty-file validation wrappers, `openclaw_prod_diag.py`, and `openclaw_fallback_smoke.py`; runbook troubleshooting updated.
- 2026-02-24: Close-session update: documented OpenClaw-first memory ingestion slice completion (contracts, bridge/job hooks, smoke coverage), added schema note (no DB migration for this slice), and updated backlog next step to OpenClaw cron/system hook wiring for jobs runner.
- 2026-02-24: Added OpenClaw-first memory ingestion module (`d_brain.memory.ingestion`) with unified `ingest_record/ingest_message_event/ingest_job_result` contracts, non-fatal storage/indexing fallback (`vault/.index_queue.jsonl`), bridge command/tutor/reflection ingestion hooks, and job runner summary ingestion hook; added `scripts/memory_ingestion_smoke.py` and runbook/integration updates.
- 2026-02-24: Added OpenClaw-first transport-agnostic job runner (`d_brain.integrations.openclaw_jobs`) with unified `run_job(...)` context/result contract for `heartbeat_summary`, `daily_digest`, and `plan_reminder_dispatch`; added `scripts/openclaw_jobs_smoke.py` with no-target, dry-run, no-items, and outbound-failure fail-safe cases; updated runbook and integration docs.
- 2026-02-24: Added process-local runtime sender registration to unified outbound (`register_runtime_sender`), enabling bridge/scheduler default paths to move from `deferred` to `sent` when OpenClaw runtime sender is wired; expanded `openclaw_digest_target_smoke.py` with `runtime_sent` case; updated runbook/integration notes.
- 2026-02-24: Close-session update: aligned context/decisions/backlog with unified OpenClaw-first outbound architecture; compileall and OpenClaw outbound/digest/scheduler smokes passed in `.venv`.
- 2026-02-24: Added unified OpenClaw-first outbound delivery module (`d_brain.integrations.openclaw_outbound`) with `send_text/send_tts/send_digest/deliver`, safe structured results (`sent/deferred/no_target/failed`), compatibility shim for existing adapter imports, bridge/scheduler integration for digest delivery path, and smoke coverage for `no_target`, `text_only`, and `tts_requested_but_empty`.
- 2026-02-24: Hardened digest target routing for OpenClaw bridge with concise RU target command UX (bind/show/on/off/clear/test and short not-configured response), explicit digest delivery diagnostics (`trigger=cron|manual`, `provider_role=cron`, `delivery_state`, `target_resolved`), persisted short delivery errors, and upgraded `openclaw_digest_target_smoke.py` to validate both `stub_deferred` and `runtime_sent` outbound modes.
- 2026-02-24: Added Windows OpenClaw operations scripts for ACL hardening and one-click local startup (`scripts/harden_openclaw_acl.ps1`, `scripts/start_openclaw_stack.ps1`, `scripts/stop_openclaw_stack.ps1`, `scripts/openclaw_doctor.ps1`) with OpenClaw-only transport defaults (`D_BRAIN_TELEGRAM_DISABLED=1`) and SSH-friendly checks.
- 2026-02-24: Implemented strict model routing policy (`d_brain.services.model_routing`) with explicit task-to-provider mapping (OpenAI reasoning, local utility defaults), utility-only local->OpenAI fallback flag, explicit OpenAI-unavailable graceful failure for main reasoning, bridge/sidecar diagnostics logging, and no-aiogram smoke coverage via `scripts/model_routing_smoke.py`.
- 2026-02-24: Added transport-agnostic voice/text routing entrypoint `d_brain.integrations.openclaw_bridge.dispatch_voice` with centralized STT language policy (default `ru`, tutor-only `en`), transcript-only warning handling, STT short RU fallback, empty TTS output text fallback, OpenClaw adapter wiring via `main_voice_handler`, and no-aiogram smoke checks (`openclaw_voice_dispatch_smoke.py`, `voice_media_preference_check.py`); compileall + smoke checks passed via `.venv`.
- 2026-02-24: Finalized transport-agnostic command entrypoint `d_brain.integrations.openclaw_bridge.dispatch_command` for `/help`, `/status`, `/plan list`, and `/plan add <title>`; kept backward-compatible alias `dispatch_openclaw_bridge_command`; OpenClaw main adapter and CLI now call the unified dispatcher; local smoke checks passed via `.venv`.
- 2026-02-24: Routed OpenClaw command handling for `/help`, `/status`, `/plan list`, and `/plan add` through the bridge dispatcher from the main skill adapter; added CLI bridge command mode and `scripts/openclaw_command_dispatch_smoke.py`; docs updated.
- 2026-02-24: Began migration to OpenClaw-only Telegram transport: added polling disable flag (`D_BRAIN_TELEGRAM_DISABLED`), conflict warning/handling in bot startup, extracted reusable UX helpers, added CLI entry (`scripts/ux_cli.py`), added mojibake-safe reply helper, fixed Help button import wiring, and extended TTS sendVoice/sendAudio validation with non-fatal fallback; docs updated. Tests not run.
- 2026-02-24: OpenClaw gateway config updated with trusted proxies for dev tunnel; OpenClaw workspace bootstrap/heartbeat filenames normalized to lowercase; summarize CLI made available on PATH via WindowsApps shim; no sidecar changes.
- 2026-02-24: Implemented Phase 3 Batch D OpenClaw adapter routing for tutor/reflect commands and reminder delivery; no sidecar changes; standalone path preserved.
- 2026-02-24: Implemented Phase 3 Batch C OpenClaw adapter routing for note/inbox workflows and news generate/deliver; no sidecar changes; standalone path preserved.
- 2026-02-24: Implemented Phase 3 Batch B OpenClaw adapter routing for low-risk writes (topic/health/project/task add); no sidecar changes; standalone path preserved.
- 2026-02-24: Implemented Phase 3 Batch A OpenClaw adapter routing for read-only/status commands (word/topic/health list, calendar views, project list, task list); no sidecar changes; standalone path preserved.
- 2026-02-24: Implemented Phase 2 OpenClaw adapter routing for /usage, /digest latest, /news latest, and /word add <word>; no sidecar changes; standalone path preserved.
- 2026-02-24: Added OpenClaw bootstrap skill stub at `vault/.claude/skills/openclaw-main` and documented its registry location; no behavior changes.
- 2026-02-24: Unified Telegram error UX for /do, /process, /weekly using the shared safe formatter; success paths unchanged; tests not run.
- 2026-02-24: Added Telegram UX ack+typing helper, applied ack/typing to long commands (/do, /process, /weekly, /news, /web, /youtube, /digest, /reflect close), and standardized safe user error formatting; tests not run.
- 2026-02-24: Localized Telegram UX to Russian (start/help/status/usages/errors), replaced reply keyboard with the custom command layout, and updated button routing; tests not run.
- 2026-02-24: MSI manual checks: npx fallback summarize extract and YouTube transcript extract both succeeded via `C:\Program Files\nodejs\npx.cmd`.
- 2026-02-24: Hardened Windows web tools: npx.cmd fallback + PATH prepend + NODE_OPTIONS=--use-system-ca for Node-based summarize runs; updated runbook smoke commands.
- 2026-02-24: Noted MSI blocker: summarize URL fetch fails behind TLS inspection until corporate root CA is installed and provided to Node.
- 2026-02-24: Expanded runbook troubleshooting steps for summarize TLS errors (corporate root CA import + NODE_EXTRA_CA_CERTS).
- 2026-02-24: Documented MSI troubleshooting for summarize TLS certificate errors (Node extra CA certs).
- 2026-02-24: Added Windows fallback for `npx.cmd` in web tools command execution and updated runbook smoke commands for MSI verification.
- 2026-02-24: Close-session: wired Telegram commands for web search and summarization, added subprocess wrapper, added Tavily MCP config, and updated docs. Local `/youtube transcript` service check failed because summarize CLI was not found; Telegram end-to-end pending.
- 2026-02-24: Added `tavily-search` and `summarize` skills (under `vault/.claude/skills`), wired Tavily MCP in `mcp-config.json`, and added `TAVILY_API_KEY` to `.env.example`. Smoke checks failed because `npx` is not installed. Errors: `npx : The term 'npx' is not recognized as the name of a cmdlet, function, script file, or operable program.`
- 2026-02-24: Added Telegram UX commands `/web search`, `/web summarize`, and `/youtube transcript` with a thin subprocess wrapper for summarize and MCP CLI for Tavily (when available). Docs updated. Smoke tests not run in this session.
- 2026-02-24: Fixed `/youtube transcript` to use `--extract` for `@steipete/summarize` (previous `--transcript` was invalid). Updated runbook command. Tests not run in this session.
- 2026-02-24: `/youtube transcript` local service check failed because summarize CLI is not available in this environment. Error: `Command not found.` Telegram end-to-end not run.
- 2026-02-24: Close-session: consolidated agent profile files under `docs/agent/`, replaced root README with a concise entry point, and updated references. Tests not run. No code changes.
- 2026-02-24: Restructured agent profile files under `docs/agent/` and replaced root README with a concise project entry point.
- 2026-02-24: Stage 6 MSI validation: `news_briefing_smoke.py` passed on MSI (`stage6_news_briefing_smoke_ok`). `news_mvp_smoke.py` failed on repeat run due to existing smoke rows and deterministic dedupe (`deduped=true`, zero deltas). This is a smoke-script idempotency issue, not a functional regression in Stage 6 news ingest.
- 2026-02-24: Made Stage 6 news MVP smoke test repeat-safe (dedupe-aware) and documented expected outputs.
- 2026-02-24: Created USER.md first pass (collaboration preferences and constraints).
- 2026-02-24: Stage 6 close-out verification completed; confirmed news actions/briefing flow/scheduler/Telegram delivery/heartbeat logging. Added USER.md first pass and aligned Stage 6 status across docs. No code changes.
- 2026-02-24: Close-session: docs-only final reconciliation; statuses aligned for Stage 18 MSI validation, MSI QA for voice/news/reminders, weekly backups configured on MSI; db-schema docs updated. No code changes.
- 2026-02-24: Final reconciliation check across docs/status/contracts/schema/runbook: updated docs to reflect MSI QA completion for voice/news/reminders, Stage 18 MSI validation, weekly backups configured on MSI, and schema_migrations/artifacts.external_id documented.
- 2026-02-24: MSI manual Telegram QA for voice/news/reminders (Stages 12/13/15/16) completed on MSI.
- 2026-02-24: TTS smoke ran with `TTS_PROVIDER=deepgram` override; passed and wrote `data/tts/tts_smoke.ogg` (size 5271 bytes).
- 2026-02-24: Close-session note: `.venv` created, deps installed; TTS smoke failed (`tts_unavailable`, provider not configured). Manual Telegram QA (Stages 12/13/15/16) not run in this session.
- 2026-02-24: Created `.venv` (it was missing) and installed deps from `requirements.txt` (MSI QA prep).
- 2026-02-24: TTS smoke output: `tts_smoke_fail`; `error_code=tts_unavailable`; `error_message=TTS provider is not configured.`
- 2026-02-24: MSI QA pass started; `scripts/tts_smoke.py` failed due to missing dependency `pydantic` (ModuleNotFoundError). Needs venv deps installed.
- 2026-02-24: Close-session note: TTS smoke passed on MSI, no architecture/schema changes, next step = venv normalization + QA pass for voice/news/reminders.
- 2026-02-24: Standardized MSI venv docs to `.venv`, tightened venv gitignore patterns, and added activation mismatch troubleshooting note.
- 2026-02-24: Added Stage 12/13 TTS smoke test script and runbook commands.
- 2026-02-24: Added Deepgram TTS adapter (REST) with config keys and graceful fallback; updated runbook notes.
- 2026-02-23: Recreated MSI venv with Python 3.12 and started the bot; Stage 18 manual Telegram verification still pending.
- 2026-02-23: Added Stage 18 manual Telegram verification checklist to the runbook.
- 2026-02-23: Close-session check-in; no code changes made in this session.
- 2026-02-23: Initialized documentation baseline.
- 2026-02-23: Added Stage 18 Projects & Tasks MVP planning notes (schema, command map, and decision records).
- 2026-02-23: Implemented Stage 18 Projects & Tasks MVP first pass (schema migration, sidecar actions, Telegram commands, smoke test, docs updates).
- 2026-02-23: Ran Stage 18 smoke test on MSI and confirmed `stage18_projects_tasks_smoke_ok`.
- 2026-02-24: Phase A hardening started: added startup config validation, lazy Deepgram import guard, and MSI quickstart/troubleshooting notes.
- 2026-02-24: Phase B hardening: added sidecar internal error mapping, Telegram handler safety, bounded retries/timeouts for Telegram and STT, and logging polish notes.
- 2026-02-24: Added Hardening Phase B QA checklist to runbook.
- 2026-02-23: Added Stage 1 migration runner, base schema migration, and sidecar scheduler skeleton.
- 2026-02-23: Drafted Stage 2 ingestion contract and added sidecar ingestion pipeline skeleton.
- 2026-02-23: Added Stage 2 ingestion smoke test script and runbook steps.
- 2026-02-23: Made Stage 2 smoke tests work with src layout and optional Deepgram config.
- 2026-02-23: Added requirements.txt and dependency install steps in runbook.
- 2026-02-23: Stage 2 completed and validated by local smoke test on MSI.
- 2026-02-23: Started Stage 3 plans/reminders with schema, dispatcher actions, and reminder tick job.
- 2026-02-23: Added Stage 3 smoke tests including parse path and parse log validation.
- 2026-02-23: Stage 3 completed and validated by local smoke tests on MSI (default + parse mode).
- 2026-02-23: Added Stage 4 English MVP schema migration and sidecar actions (words, topics, sessions).
- 2026-02-23: Added Stage 4 English MVP smoke test script and runbook steps.
- 2026-02-23: Documented Stage 4 English MVP sidecar actions in OpenClaw integration notes.
- 2026-02-23: Stage 4 English MVP completed and validated by local smoke test on MSI.
- 2026-02-23: Implemented Stage 5 Reflection MVP first pass (schema, sidecar actions, smoke test script).
- 2026-02-23: Stage 5 Reflection MVP completed and validated by local smoke test on MSI.
- 2026-02-23: Started Stage 6 News MVP first pass (schema, sidecar actions, smoke test, docs updates).
- 2026-02-23: Stage 6 News MVP first pass completed and validated by local smoke test on MSI.
- 2026-02-23: Implemented Stage 6 News MVP second pass slice (briefing schema, manual briefing, summaries, smoke test). Local smoke test pending on MSI.
- 2026-02-23: Implemented Stage 7 first pass (digests, heartbeat logs, sidecar actions, utility logging hooks, smoke test, docs updates).
- 2026-02-23: Stage 7 first pass validated by local smoke test on MSI.
- 2026-02-23: Implemented Stage 8 first pass (codex usage logs, limits settings, advisory status, smoke test, docs updates).
- 2026-02-23: Stage 8 first pass validated by local smoke test on MSI.
- 2026-02-23: Implemented Stage 9 Health MVP first pass (schema, sidecar actions, smoke test, docs updates).
- 2026-02-23: Stage 9 Health MVP first pass validated by local smoke test on MSI.
- 2026-02-23: Implemented Stage 10 Idea Research first pass (schema, sidecar actions, smoke test, docs updates).
- 2026-02-23: Stage 10 Idea Research first pass validated by local smoke test on MSI.
- 2026-02-23: Implemented Stage 11 Telegram UX wiring MVP (text-only command routing, thin adapter, manual checklist). Manual Telegram verification on MSI pending before marking completed.
- 2026-02-23: Started Stage 12 Voice English Tutor MVP (STT/TTS adapters, Telegram tutor flow wiring, and runbook updates in progress).
- 2026-02-23: Implemented Stage 12 first pass wiring (provider-agnostic STT/TTS adapters, tutor session flow, Telegram /tutor commands, voice/text routing, runbook and integration docs updates). MSI manual Telegram verification completed.
- 2026-02-23: Implemented Stage 13 Reflection Voice Loop MVP first pass (reflection voice service, reflection mode routing for voice/text, STT/TTS reuse, and documentation updates). MSI manual Telegram verification completed.
- 2026-02-24: Started Stage 14 first pass (Books/Philosophy/Knowledge UX) with minimal note categories, sidecar actions, Telegram wiring, and docs updates.
- 2026-02-24: Added note_categories migration, Stage 14 sidecar actions (books/philosophy/inbox), Telegram text commands, and documentation updates. Manual Telegram verification on MSI pending.
- 2026-02-23: Implemented Stage 15 first pass (news briefing generation + Telegram delivery jobs, manual commands, scheduler script, systemd units, and docs updates).
- 2026-02-23: Made Telegram delivery dependency import-safe with lazy `httpx` loading and documented the dependency note in the runbook.
- 2026-02-23: Implemented Stage 16 first pass (reminder delivery job, calendar-style Telegram views, heartbeat traceability, and docs updates). MSI manual Telegram verification completed.
- 2026-02-23: Implemented Stage 16B Backup & Restore MVP (backup config, scheduler job, backup script, docs snapshot ZIP, and Windows Task Scheduler runbook section). Manual MSI verification pending (backup creation + restore integrity check).

 - 2026-02-25: Patched Windows `gateway/ws` pretty logging mojibake risk in the installed OpenClaw runtime bundles (outside repo source) by hardening the `gateway/ws` formatter path in `push-apns-*.js` to use ASCII-safe directional/status tokens (`<-`, `->`, `<->`, `OK`, `ERR`), remove unicode ellipsis in compact IDs/previews (`...`), and write a sanitized/plain file-log message while preserving a separate pretty `consoleMessage` for console output. Added compact helpers in the runtime bundle patch (`stripAnsi`, `normalizeLogForWindows`) and routed `logWsInfoLine(...)` plus parse-error warnings through the sanitized file-message path. Verification PASS: `node.exe --check` on patched `push-apns-BRGDsNym.js` and `push-apns-A3yh2vIh.js`; source inspection confirms `gateway/ws` formatter uses ASCII tokens and file-message sanitization. Live file-log capture of a new post-patch `gateway/ws` pretty line is still pending, so runtime evidence is partial.

## Next
- Stage 6: News MVP (second pass: summaries + briefing pipeline + exactly 5 key events).
- Confirm persistence choices and deployment workflow.

- 2026-02-24: Fixed Telegram RU UI encoding and syntax regressions: restored /start and /help RU text in telegram_ux, removed duplicate EN handlers in commands, normalized reply keyboard labels and button handlers, and repaired multiline string formatting; compileall passed.

- 2026-02-24: Implemented scheduled digest target routing via OpenClaw bridge with persistent target mapping (`/digest target here|show|on|off|clear|test`), scheduled delivery states (`sent/no_target/failed/deferred`), and deterministic target smoke coverage.


## 2026-02-25 - Embedded voice bridge runtime stabilization (wrapper-first)\n- Fixed scripts/openclaw_live_voice_bridge_cli.py adapter loader to register module in sys.modules before exec_module (prevents dataclass import crash in adapter import path).\n- Added safe placeholder TELEGRAM_BOT_TOKEN injection in wrapper runtime when absent (embedded local invocation only) to allow settings initialization for STT path.\n- Ensured local wrapper runtime has required Python deps (pydantic, pydantic-settings, httpx, iogram, deepgram-sdk, etc. via 
equirements.txt).\n- Created missing runtime directory ault/.sessions used by adapter session storage in local embedded invocations.\n- Verification: openclaw_live_voice_bridge_cli_smoke.py PASS, openclaw_embedded_media_redirect_smoke.py PASS, live wrapper redirect call on ile_129...ogg returned ok=true, selectedPath=wrapper_cli_bridge -> d_brain_openclaw_bridge, stt_multipass=true.\n- Remaining: verify full end-to-end behavior in live Telegram transport path (not just direct wrapper CLI invocation).

## 2026-02-25 - Mixed-language STT candidate selection hardening\n- Updated src/d_brain/integrations/openclaw_bridge.py multipass candidate selector to better preserve RU segments in RU+EN mixed speech.\n- Heuristic updates:\n  - prefer mixed-script candidates when both scripts are observed across candidate set;\n  - add RU-preservation bonus when Cyrillic evidence exists in candidate pool;\n  - penalize script/lang mismatch (
u pass without Cyrillic, en pass without Latin).\n- Goal: reduce cases where English-only candidate suppresses Russian fragments in mixed utterances.\n- Verification: wrapper redirect live call remains ok=true for audio media path; additional mixed live Telegram confirmation pending with user test phrase.
- 2026-02-25: Close-session verification pass for embedded Telegram voice wrapper-first path. Ran `openclaw_live_voice_bridge_cli_smoke.py`, `openclaw_embedded_media_redirect_smoke.py`, `openclaw_voice_dispatch_smoke.py`, and `openclaw_voice_source_select_smoke.py` (all PASS). Tail-scoped `openclaw_live_voice_path_verdict.py` on `artifacts/openclaw_live_voice_bridge_verify.log` + session tail returned `LIVE_PATH_CONFIRMED_BRIDGE`, but strict acceptance evidence is still incomplete because a single request-scoped live excerpt proving the full runtime guard -> redirect -> wrapper -> adapter -> bridge marker chain was not captured in this session. Session JSONL live excerpt confirms redirect `selectedPath=wrapper_cli_bridge` -> wrapper `selectedPath=d_brain_openclaw_bridge` with adapter diagnostics (`stt_language=ru`, `stt_multipass=true`). Remaining live work: capture one fresh mixed RU+EN voice note with a request-scoped marker chain (including runtime exec-guard marker and bridge `telegram_voice_pipeline`/`stt_multipass_selected`) and finalize the 5-point acceptance table with direct proof.
- 2026-02-25: Final fix pass (runtime guard marker contract + strict verdict helper). Patched installed OpenClaw `pi-embedded` runtime bundles (outside repo source) so the embedded audio exec-guard proof/error markers include `traceStage=runtime.exec_guard` and `requestId`, and the fail-closed runtime error uses `errorCode=wrapper_bridge_unavailable` (instead of `wrapper_cli_unavailable`). Hardened `scripts/openclaw_live_voice_path_verdict.py` to require a same-`requestId` full chain (runtime -> redirect -> wrapper -> adapter pre/post -> bridge `telegram_voice_pipeline` + `stt_multipass_selected`) before returning `LIVE_PATH_CONFIRMED_BRIDGE`; updated verdict smoke accordingly. Verification PASS: `node.exe --check` on both patched `pi-embedded` bundles, `openclaw_live_voice_path_verdict_smoke.py`, `openclaw_embedded_media_redirect_smoke.py`, `openclaw_live_voice_bridge_cli_smoke.py`, `openclaw_voice_source_select_smoke.py`, `openclaw_voice_dispatch_smoke.py`. Existing tail captures now correctly return `INCONCLUSIVE` under the stricter verdict (no same-requestId full chain in historical evidence). Remaining live work: restart OpenClaw and capture one fresh mixed RU+EN voice sample to produce a request-scoped full-chain excerpt and finalize strict acceptance.
- 2026-02-25: Final-mile same-requestId chain closure patch (requestId continuity only). Added audio-path fail-closed guards for missing trace ID in redirect/wrapper/adapter (`errorCode=request_id_missing_in_chain`, no silent fallback) and improved `openclaw_live_voice_path_verdict.py` requestId extraction to parse escaped `requestId`/`request_id` and escaped `traceStage` fields from session/log JSONL lines. Focused verification PASS: `openclaw_embedded_media_redirect_smoke.py`, `openclaw_live_voice_bridge_cli_smoke.py`, `openclaw_live_voice_path_verdict_smoke.py`; direct check confirms redirect returns `request_id_missing_in_chain` on audio prompt with empty requestId.
